<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="BSMM2.Models.Matches.SingleMatch.SingleMatchPage"
			 Title="{Binding Title}">
	<ContentPage.Content>
		<StackLayout VerticalOptions="Center">
			<Label Text="Enter the Result" />
			<ListView x:Name="Results"
					  SelectedItem="{Binding SelectedItem}"
					  ItemsSource="{Binding Items}"
					  ItemTapped="OnResultTapped">
				<ListView.ItemTemplate>
					<DataTemplate>
						<TextCell Text="{Binding Name}" TextColor="Black" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
			<Editor Text="{Binding LifePoint1}" Keyboard="Numeric" IsVisible="{Binding EnableLifePoint}" />
			<Editor Text="{Binding LifePoint2}" Keyboard="Numeric" IsVisible="{Binding EnableLifePoint}" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>